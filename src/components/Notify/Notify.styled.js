import styled from 'styled-components'

export const Wrapper = styled.div`
  position: fixed;
  bottom: 25px;
  right: 25px;
  background: #4caf50;
  color: #fff;
  border-radius: 7px;
  padding: 10px;
  display: flex;
  align-items: center;
  gap: 6px;
  z-index: 20;

  svg {
    font-size: 2.2rem;
  }

  p {
    font-size: 1.6rem;
  }
`
